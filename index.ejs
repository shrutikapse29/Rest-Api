<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog Website</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f9f9f9;
      margin: 0;
      padding: 20px;
    }

    .card {
      background-color: #fff;
      border: 1px solid #ddd;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      padding: 20px;
      border-radius: 10px;
      margin-bottom: 20px;
      max-width: 800px;
      margin-left: auto;
      margin-right: auto;
    }

    .card h1 {
      margin-top: 0;
      font-size: 24px;
      color: #333;
    }

    .card h3 {
      margin: 8px 0;
      font-weight: normal;
      color: #555;
    }

    .card img {
      max-width: 100%;
      border-radius: 8px;
      margin-top: 10px;
    }
    button{
        padding:5px;
        border-radius:4px;
        background-color:blue;
        color:white;
    }
  </style>
</head>
<body>
  <h1 style="text-align:center;">Blog Website</h1>

  <% for (let blog of blogdata) { %>
    <div class="card">
      <h1><%= blog.title %></h1>
      <h3><strong>Author:</strong> <%= blog.author %></h3>
      <h3><strong>Date:</strong> <%= blog.date %></h3>
      <h3><strong>Summary:</strong> <%= blog.summary %></h3>
      <h3><strong>Content:</strong> <%= blog.content %></h3>
      <h3><strong>Tags:</strong> <%= blog.tags.join(", ") %></h3>
      <img src="<%= blog.image %>" alt="Blog image">
      <br>
      <br>
      <button>Add new posts</button>
    </div>
  <% } %>

</body>
</html>
